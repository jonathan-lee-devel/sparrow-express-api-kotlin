package io.jonathanlee.sparrowexpressapikotlin.enums.organization

enum class OrganizationMembershipStatus {
    APPROVED,
    FORBIDDEN,
    FAILURE,
    ORGANIZATION_DOES_NOT_EXIST,
    USER_ALREADY_MEMBER,
    REQUEST_ALREADY_EXISTS,
    AWAITING_APPROVAL,
    NOT_FOUND
}
